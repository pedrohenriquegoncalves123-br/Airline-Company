
/* window.addEventListener('scroll', () => {
    const menu = document.querySelector('.menu');

    if (window.scrollY > 50) {
        menu.classList.add('menu-rolagem');
    } else {
        menu.classList.remove('menu-rolagem');
    }
});
 */